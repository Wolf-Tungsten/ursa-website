{{!< default}} 

<main id="gh-main" class="gh-main gh-outer">
    <div class="gh-inner">

        <div class="ursa-research-area">
            <h1>Research Areas</h1>
            <div class="ursa-research-area-grid">
                <div class="ursa-research-area-item">
                    <img src="{{asset "images/interest/dsa.png"}}" class="hero-animation transition-delay-0"/>
                    <h2 class="hero-animation transition-delay-1">Domain-specific Accelerators</h2>
                    <p class="hero-animation transition-delay-2">
                        Buidling Domain-Specific Accelerators and Supercomputer for Emerging Applications
                    </p>
                </div>
                <div class="ursa-research-area-item middle-shadow">
                    <img src="{{asset "images/interest/pim.png"}}" class="hero-animation transition-delay-0"/>
                    <h2 class="hero-animation transition-delay-1">Memory-centric Architectures</h2>
                    <p class="hero-animation transition-delay-2">
                        Novel Memory Architecture (3D integrated circuits) for Data-centric Applications
                    </p>
                </div>
                <div class="ursa-research-area-item">
                    <img src="{{asset "images/interest/gene.png"}}" class="hero-animation transition-delay-0"/>
                    <h2 class="hero-animation transition-delay-1" style="line-height: 2.5em;">AI+Genomics</h2>
                    <p class="hero-animation transition-delay-2">
                        Bring together the power of genomics, docking, simulation, imaging
                        to develop AI applications for the the next generation of drug
                        discovery processes
                    </p>
                </div>
            </div>
        </div>

        {{!-- {{^is "paged"}}
        {{#get "posts" limit="1"}}
        {{#foreach posts}}
        <article class="gh-latest gh-card {{post_class}}">
            <a class="gh-card-link" href="{{url}}">
                <header class="gh-card-header">
                    <div class="gh-article-meta">
                        <span class="gh-card-date">Latest — <time datetime="{{date format="
                                YYYY-MM-DD"}}">{{date}}</time></span>
                    </div>

                    <h2 class="gh-article-title gh-card-title">{{title}}</h2>
                </header>

                <p class="gh-article-excerpt">{{excerpt}}</p>

                <footer class="gh-card-meta">
                    <span class="gh-card-meta-wrapper">
                        <span class="gh-card-duration">{{reading_time}}</span>

                        {{^has visibility="public"}}
                        {{> icons/star}}
                        {{/has}}
                    </span>
                </footer>
            </a>
        </article>
        {{/foreach}}
        {{/get}}
        {{/is}} --}}

        <div class="gh-wrapper">
            <section class="gh-section">
                <h2 class="gh-section-title">Group News</h2>

                <div class="gh-feed">
                    {{^is "paged"}}
                    {{#get "posts" filter="tag:'group-news'" limit="all"}} 
                    {{#foreach posts from="1"}}
                    {{> loop}}
                    {{/foreach}}
                    {{else}}
                    {{#foreach posts}}
                    {{> loop}}
                    {{/foreach}}
                    {{/get}} 
                    {{/is}}
                </div>

                {{!-- {{#get "posts" filter="tag:'group-news'" limit="all" as |groupnews|}} 
                {{#match groupnews.length "=" 1}}
                Additional issues will be published soon.
                {{/match}}
                {{/get}}

                <button class="gh-loadmore gh-btn">Load more issues</button> --}}
            </section>

            <aside class="gh-sidebar">
                <section class="gh-section">

                    <div class="gh-about">
                        {{#if @site.icon}}
                        <img class="gh-about-icon" src="{{@site.icon}}" alt="{{@site.title}}">
                        {{/if}}

                        <section class="gh-about-wrapper">
                            <h3 class="gh-about-title">{{@site.title}}</h3>

                            {{#if @site.description}}
                            <p class="gh-about-description">{{@site.description}}</p>
                            {{/if}}
                        </section>
                    </div>

                    <h2 class="gh-section-title" style="margin-top:var(--gap);">Connect with Us</h2>

                    <section class="gh-about-wrapper">
                        <div class="ursa-about-item">
                            <h5 class="gh-about-description">Address</h5>
                            <p>
                                ICT, CAS 
                                No.6 Kexueyuan South Road Zhongguancun, Haidian District
                                Beijing, China
                            </p>
                        </div>
                        <div class="ursa-about-item">
                            <h5 class="gh-about-description">Email</h5>
                            <p>
                                lixueqi@ict.ac.cn
                            </p>
                        </div>
                        <div class="ursa-about-item">
                            <div></div>
                            <a href="https://clustrmaps.com/site/1boy7" title="Visit tracker">
                                <img src="//clustrmaps.com/map_v2.png?cl=0e1633&w=500&t=tt&d=PeWhojyi857c7zd6QCn3Lw3EZrCG5hQzIOKj4e-RroQ&co=f6f6f6&ct=cdd4d9" />
                            </a>
                        </div> 
                        
                    </section>
                    {{!-- {{^if @member.paid}}
                    <div class="gh-signup">
                        {{^if @member}}
                        <p class="gh-signup-description">Sign up now to get access to the library of members-only
                            issues.</p>

                        <a class="gh-subscribe-input" href="#/portal/signup" data-portal="signup">
                            <div class="gh-subscribe-input-text">
                                {{> icons/email}}
                                jamie@example.com
                            </div>
                            <div class="gh-subscribe-input-btn">Subscribe</div>
                        </a>
                        {{else}}
                        <p class="gh-signup-description">Upgrade to a paid account to get full access.</p>
                        <a class="gh-signup-btn gh-btn gh-primary-btn" href="#/portal/account/plans"
                            data-portal="account/plans">Upgrade now</a>
                        {{/if}}
                    </div>
                    {{/if}} --}}
                </section>

                {{!-- {{#get "posts" filter="featured:true" limit="all" as |featured|}}
                {{#if featured}}
                <section class="gh-section">
                    <h3 class="gh-section-title">Featured</h3>

                    <div class="gh-featured gh-feed">
                        {{#foreach featured}}
                        {{> loop}}
                        {{/foreach}}
                    </div>
                </section>
                {{/if}}
                {{/get}} --}}

                {{!-- {{#get "tags" include="count.posts" limit="all" as |topic|}}
                <section class="gh-section">
                    <h3 class="gh-section-title">Topics</h3>

                    <div class="gh-topic">
                        {{#foreach topic}}
                        <a class="gh-topic-item" href="{{url}}">
                            <h3 class="gh-topic-name">{{name}}</h3>
                            <span class="gh-topic-count">
                                {{plural count.posts empty="0 issues" singular="% issue" plural="% issues"}}
                            </span>
                        </a>
                        {{/foreach}}
                    </div>
                </section>
                {{/get}} --}}
            </aside>
        </div>
    </div>
    </main>